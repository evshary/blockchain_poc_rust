pub struct Connection;

// TODO: Create protobuf for command

impl Connection {
    pub fn new() -> Self {
        Self {}
    }

    // TODO: The function we need
    // Connection
    //  - connect
    //  - send
    //  - send(command)
    //  - recv(commnad)
    //  - peer_list
    //  - transfer
    //  - get_peer
}
